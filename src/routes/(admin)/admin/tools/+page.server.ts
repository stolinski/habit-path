
export const actions = {
	async default({ locals }) {	
		// const all_users_habits_for_recording = await db.query.user.findMany({
		// 	with: {
		// 		habits: true
		// 	}
		// })

  // for (const user of all_users_habits_for_recording) {
	// 	let index = 1;
  //   for (const habit of user.habits) {
			
  //     const new_order = index * 10;
	// 		await db.update(habits).set({
	// 			updated_at: new Date(),
	// 			order: new_order,
	// 		}).where(eq(habits.id, habit.id))
	// 		// Last thing
	// 		index += 1;
  //   }
  // }
  // console.log('All habits have been processed');


	},
};
